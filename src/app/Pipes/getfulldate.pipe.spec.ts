import { GetfulldatePipe } from './getfulldate.pipe';

describe('GetfulldatePipe', () => {
  it('create an instance', () => {
    const pipe = new GetfulldatePipe();
    expect(pipe).toBeTruthy();
  });
});
