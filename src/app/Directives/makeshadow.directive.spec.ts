import { MakeshadowDirective } from './makeshadow.directive';

describe('MakeshadowDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeshadowDirective();
    expect(directive).toBeTruthy();
  });
});
